#include <stdio.h>
/**
 * main - use putchar function
 * Description: program that prints all the possible combinations
 * Return: always 0
 */
int main(void)
{
int ch;
for (ch = 48; ch <= 57; n++)
{
putchar(ch);
if (ch != 57)
{

putchar(44);
putchar(32);
}
putchar(10);
return (0);
}
}
