#include <stdio.h>
/**
 * main - prints exactly letter followed by new line
 * Description: use the function printf
 * Return: always 0
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
